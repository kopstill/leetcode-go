package leetcode

import (
	"fmt"
	"testing"
)

func TestLongestPalindromeI(t *testing.T) {
	// s := "xxxbbaceddab"
	// s := "abccccdd"
	// s := "ccc"
	s := "ababababa"
	fmt.Println(longestPalindromeI(s))
}
